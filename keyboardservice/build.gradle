apply plugin: 'com.android.application'

android {
    compileSdkVersion 29
    buildToolsVersion '28.0.3'

    defaultConfig {
        applicationId 'com.android.aisinput'
        minSdkVersion 15
        targetSdkVersion 29
        versionCode 2
        versionName "2.0"
        signingConfig signingConfigs.debug
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
}
